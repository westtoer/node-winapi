/*jslint node: true*/

"use strict";
var fs = require('fs'),
    path = require('path'),
    util = require('util'),
    moment = require('moment');



module.exports.doSplit = function (size, dirname, tplPageName, fname) {
    console.log("todo split json from %s into %s/%s pages of size %d", fname, dirname, tplPageName, fname);
};

